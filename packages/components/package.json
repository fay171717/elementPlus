{
  "name": "@elementPlus/components",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cross-env NODE_ENV=test pnpm --filter @elementPlus/hooks build && vitest --run --coverage",
    "test:watch": "cross-env NODE_ENV=test pnpm --filter @elementPlus/hooks build && vitest --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vitest": "^1.4.0"
  },
  "dependencies": {
    "@popperjs/core": "2.11.8",
    "async-validator": "4.2.5"
  }
}
